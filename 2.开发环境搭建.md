# 第2章 开发环境搭建

机器人程序并不是只要“代码写对了”就能运行。操作系统版本、编译器、Python 依赖、设备权限和网络配置中的任何一项不一致，都可能让同一份代码在不同电脑上表现出完全不同的结果。

因此，开发环境不是正式开发之前随便应付的一道准备工作，而是机器人系统的一部分。本章将从一台尚未安装 Linux 的计算机开始，搭建能够学习 ROS2、编译 RC2026 工程并连接真机设备的 Ubuntu 开发环境。

> 本章统一使用**原生 Ubuntu 22.04 LTS**，不讲解虚拟机和 WSL。Windows 只用于制作 Ubuntu 启动盘。RC2026 项目采用 ROS2 Humble，它的官方目标平台就是 Ubuntu 22.04；不要因为 Ubuntu 24.04 或 26.04 数字更大，就擅自更换系统版本。

## 本章学习目标

完成本章后，你应当能够：

- 独立安装并完成 Ubuntu 22.04 的基本配置；
- 理解 Linux 的目录、用户、权限、软件包和环境变量；
- 使用终端完成文件管理、软件安装和基本故障排查；
- 安装 Git、编译工具和 ROS2 Humble；
- 理解 ROS2 工作空间的构建与环境加载过程；
- 获取、构建并验证 RC2026 上位机工程；
- 检查串口、相机、雷达网络和远程登录环境。

## 2.1 确定统一的软件版本

### 2.1.1 本教材的环境基线

本教材与 RC2026 项目采用以下环境：

| 项目 | 统一版本或选择 | 说明 |
| --- | --- | --- |
| 操作系统 | Ubuntu 22.04 LTS 64 位 | 代号 Jammy Jellyfish |
| ROS | ROS2 Humble Hawksbill | 与 Ubuntu 22.04 配套的长期支持版本 |
| 终端 Shell | Bash | Ubuntu 默认 Shell |
| Python | Python 3.10 | Ubuntu 22.04 自带版本 |
| C/C++ 编译器 | Ubuntu 22.04 仓库提供的 GCC/G++ | 不自行替换系统编译器 |
| 构建工具 | CMake、colcon | 分别用于 C/C++ 与 ROS2 工作空间构建 |
| 主要语言 | Python、C++ | 对应项目中的算法节点和底层模块 |
| 项目平台 | x86_64/amd64 | RC2026 真机使用 AMD 小主机 |

ROS2 的发行版与 Ubuntu 版本通常是一一对应的。对新生而言，最重要的原则是：

> **先按照项目确定 ROS2 版本，再选择它支持的 Ubuntu 版本。**

截至本章编写时，ROS2 Humble 的官方支持周期到 2027 年 5 月。RC2026 项目已经基于 Humble 开发和验证，因此本教材继续使用这套组合。未来项目整体迁移时，应由团队统一升级，而不是由每位成员自行选择版本。

### 2.1.2 为什么不用“最新版本”

机器人项目依赖大量驱动和第三方库，例如 RealSense、Livox、FAST-LIO2、Gazebo 和各种 ROS2 功能包。操作系统升级后，内核、Python、Gazebo 或编译器也会变化，原本可以工作的驱动不一定仍然兼容。

开发环境的目标不是追求版本号最大，而是做到：

- 团队成员使用相同版本；
- 真机和开发机尽可能一致；
- 依赖可以重复安装；
- 出现问题时能够复现；
- 比赛前不发生没有必要的大版本变化。

## 2.2 安装前准备

### 2.2.1 硬件建议

下面的配置不是 Ubuntu 的最低要求，而是算法组开发的实际建议：

| 项目 | 基础学习 | 视觉、仿真和真机开发建议 |
| --- | --- | --- |
| CPU | 4 核 64 位处理器 | 6 核以上现代 x86_64 处理器 |
| 内存 | 8 GB | 16 GB 或更多 |
| 可用磁盘 | 50 GB | 100 GB 或更多 |
| USB | 普通 USB 接口 | 至少一个稳定的 USB 3.x 接口 |
| 网络 | Wi-Fi | Wi-Fi 加千兆有线网口 |
| 显卡 | 集成显卡即可 | 根据模型推理方式决定是否需要独立显卡 |

RC2026 真机运行平台为 AMD Ryzen 7 8845HS 小主机，当前 YOLO 模型可以使用 CPU 推理。因此，本章不会把 NVIDIA 显卡和 CUDA 作为基础环境的强制要求。

### 2.2.2 推荐安装方式

用于真机的上位机应当直接安装原生 Ubuntu。推荐顺序为：

1. **单独使用一块硬盘安装 Ubuntu**：最容易维护，也最不容易误删其他系统的数据；
2. **整台计算机只安装 Ubuntu**：适合专门用于算法组开发的设备；
3. **与已有系统共用硬盘**：涉及磁盘分区和引导，新生应由有经验的成员现场协助。

本教材不展开手动分区。第一次安装时，优先使用单独硬盘和安装器提供的自动分区。


### 2.3.5 第一次启动

安装完成并重新启动后，拔出安装 U 盘。打开终端的快捷键是：

```text
Ctrl + Alt + T
```

先确认系统版本和处理器架构：

```bash
lsb_release -a
uname -m
```

期望看到：

- Ubuntu 版本为 `22.04`；
- 代号为 `jammy`；
- 架构为 `x86_64`。

如果版本不符，应当在安装 ROS2 和其他依赖之前解决。不要抱着“先装一下试试”的想法继续堆叠错误环境。

## 2.4 完成 Ubuntu 基础配置

### 2.4.1 更新系统

ROS2 Humble 官方特别要求 Ubuntu 22.04 在安装 ROS2 前完成系统更新。依次执行：

```bash
sudo apt update
sudo apt full-upgrade -y
sudo reboot
```

这里三个命令分别表示：

- `apt update`：刷新软件包目录，不会直接升级软件；
- `apt full-upgrade`：安装可用系统更新，并处理依赖变化；
- `reboot`：重新启动，使新内核和系统组件生效。

输入 `sudo` 密码时，终端不会显示星号或圆点，这是正常现象。输入完成后直接按回车。

### 2.4.2 设置时间与时区

机器人中的图像、点云、里程计和日志都依赖时间戳。系统时间错误会导致传感器无法同步，甚至让 TF 查询失败。

```bash
timedatectl status
sudo timedatectl set-timezone Asia/Shanghai
sudo timedatectl set-ntp true
timedatectl status
```

最后检查时区是否正确，以及系统时钟是否已经同步。多台机器人或多台计算机协作时，还需要统一网络时间，后续部署章节会进一步讲解。

### 2.4.3 检查系统资源

```bash
lscpu
free -h
df -h /
uname -r
```

分别用于查看 CPU、内存、根分区剩余空间和 Linux 内核版本。编译失败、程序突然退出或模型无法加载时，这些信息是最先需要检查的基础条件。

## 2.5 认识终端与 Linux 目录

### 2.5.1 命令提示符

打开终端后通常会看到类似内容：

```text
student@r2-upper-01:~$
```

它表示：

- `student`：当前用户名；
- `r2-upper-01`：计算机名；
- `~`：当前位于用户主目录；
- `$`：当前是普通用户。

教程代码块中的 `$` 如果用于表示提示符，不需要输入。本教材的命令块通常不写提示符，可以直接逐行执行。

### 2.5.2 Linux 目录结构

Linux 从唯一的根目录 `/` 开始组织文件，而不是使用 `C:`、`D:` 盘符。

| 路径 | 作用 |
| --- | --- |
| `/` | 整个文件系统的根目录 |
| `/home/用户名` | 普通用户的主目录，`~` 是它的简写 |
| `/opt` | 可选的大型软件，ROS2 默认安装在这里 |
| `/etc` | 系统和服务配置文件 |
| `/dev` | 串口、相机、磁盘等设备文件 |
| `/usr` | 系统程序、库和共享资源 |
| `/tmp` | 临时文件，不能保存重要资料 |
| `/var/log` | 部分系统服务日志 |

Linux 路径区分大小写。`Project`、`project` 和 `PROJECT` 是三个不同的名称。

### 2.5.3 绝对路径与相对路径

绝对路径从 `/` 开始：

```text
/home/student/Projects/GAFA-Artlnnov.RC2026
```

相对路径从当前目录开始：

```text
ros2_vision_project/ros2_ws
```

几个常见简写：

| 写法 | 含义 |
| --- | --- |
| `~` | 当前用户主目录 |
| `.` | 当前目录 |
| `..` | 上一级目录 |
| `-` | `cd` 命令中的上一次所在目录 |

### 2.5.4 常用文件命令

| 命令 | 作用 | 示例 |
| --- | --- | --- |
| `pwd` | 显示当前目录 | `pwd` |
| `ls` | 列出目录内容 | `ls -al` |
| `cd` | 切换目录 | `cd ~/Projects` |
| `mkdir` | 创建目录 | `mkdir -p ~/Projects` |
| `touch` | 创建空文件或更新时间 | `touch notes.txt` |
| `cp` | 复制文件 | `cp a.txt b.txt` |
| `mv` | 移动或重命名 | `mv old.txt new.txt` |
| `rm` | 删除文件 | `rm -i notes.txt` |
| `less` | 分页查看文本 | `less README.md` |
| `head` | 查看文件开头 | `head README.md` |
| `tail` | 查看文件结尾 | `tail -f program.log` |
| `find` | 按条件寻找文件 | `find src -name package.xml` |
| `grep` | 搜索文本 | `grep -R "TODO" src` |

练习创建一个目录：

```bash
mkdir -p ~/rc_course/chapter02
cd ~/rc_course/chapter02
touch hello.txt
ls -al
pwd
```

使用 `Tab` 键补全命令和路径，使用上下方向键查看历史命令。路径较长时不要反复手敲，这能减少大量拼写错误。

> `rm` 删除的文件通常不会进入回收站。不要复制执行来源不明的 `sudo rm -rf ...` 命令，也不要在不清楚当前目录时递归删除文件。

### 2.5.5 管道与重定向

管道 `|` 会把前一个命令的输出交给后一个命令：

```bash
ros2 topic list | grep vision
```

重定向会把输出写入文件：

```bash
ros2 topic list > topics.txt
```

单个 `>` 会覆盖已有文件，`>>` 会追加到文件末尾。执行前应确认目标文件是否可以覆盖。

### 2.5.6 终止程序和查看帮助

终端程序持续运行时，通常使用：

```text
Ctrl + C
```

这会向程序发送中断信号，不等于关闭整个终端。查看帮助可以使用：

```bash
命令 --help
man 命令
```

例如：

```bash
ls --help
man ls
```

## 2.6 用户、权限和软件包

### 2.6.1 普通用户与 root

Linux 的最高权限用户叫 `root`。普通用户不能随意修改系统文件，需要在确实执行系统管理操作时使用 `sudo` 临时提权。

```bash
sudo apt update
```

正确的习惯是：

- 只在安装软件、修改系统服务或设备规则时使用 `sudo`；
- 不使用 `sudo` 编译普通 ROS2 工作空间；
- 不使用 `sudo` 运行自己的视觉和决策节点；
- 不为了绕过权限错误而给整个项目目录开放写权限；
- 执行陌生的 `sudo` 命令前先理解它会修改什么。

### 2.6.2 文件权限

查看权限：

```bash
ls -l
```

输出开头类似：

```text
-rwxr-xr--
```

其中 `r`、`w`、`x` 分别表示读取、写入和执行。让脚本可以执行：

```bash
chmod +x script.sh
./script.sh
```

如果脚本本身可以用 `bash script.sh` 运行，就不一定需要修改执行权限。

### 2.6.3 使用 apt 管理软件

Ubuntu 使用 `apt` 安装和更新软件：

```bash
sudo apt update
sudo apt install package-name
sudo apt remove package-name
apt search keyword
apt show package-name
```

`apt update` 只更新软件列表；`apt upgrade` 才会升级已安装软件。遇到 `Unable to locate package` 时，先检查是否执行过 `apt update`，再检查系统版本和软件源，不要立刻从随机网站下载 `.deb` 文件。

### 2.6.4 环境变量与 PATH

环境变量用于向程序传递配置。`PATH` 决定终端到哪些目录寻找命令：

```bash
echo "$PATH"
which python3
python3 --version
```

`source` 会在当前终端中执行一个环境脚本：

```bash
source /opt/ros/humble/setup.bash
```

它不会安装 ROS2，只是让当前终端知道 ROS2 的命令、库和功能包在哪里。新终端默认不会继承另一个终端临时执行的 `source`。

## 2.7 安装基础开发工具

### 2.7.1 常用软件包

```bash
sudo apt update
sudo apt install -y build-essential cmake git git-lfs curl wget
sudo apt install -y python3-pip python3-venv python3-dev
sudo apt install -y tree htop net-tools iputils-ping usbutils v4l-utils
```

这些工具主要用于：

| 工具 | 用途 |
| --- | --- |
| `build-essential` | 安装 GCC、G++、Make 等基础编译工具 |
| `cmake` | 配置 C/C++ 工程 |
| `git` | 版本控制和代码协作 |
| `git-lfs` | 管理模型等大文件 |
| `curl`、`wget` | 下载和测试网络资源 |
| `python3-pip` | 安装无法通过 apt 获取的 Python 包 |
| `python3-venv` | 创建隔离的 Python 环境 |
| `tree` | 以树形显示目录 |
| `htop` | 查看进程与资源占用 |
| `usbutils` | 提供 `lsusb` |
| `v4l-utils` | 检查普通 USB 相机 |

验证关键工具：

```bash
gcc --version
g++ --version
cmake --version
git --version
python3 --version
```

### 2.7.2 不要破坏系统 Python

ROS2 Humble 的许多工具依赖 Ubuntu 自带的 Python 3.10 和系统 Python 包。以下做法很容易破坏环境：

- 使用 `sudo pip install ...` 覆盖系统包；
- 把 `/usr/bin/python3` 改成其他 Python 版本；
- 全局升级 `setuptools`、`empy` 等 ROS2 构建依赖；
- 把 Anaconda 的 Python 永久放在系统 Python 前面；
- 在多个环境中反复设置 `PYTHONPATH`。

与 ROS2 无关的 Python 实验应使用虚拟环境：

```bash
python3 -m venv ~/venvs/experiment
source ~/venvs/experiment/bin/activate
python -m pip install --upgrade pip
deactivate
```

这里的 Python 虚拟环境只是隔离 Python 包，与操作系统虚拟机不是一回事。RC2026 ROS2 工作空间是否使用虚拟环境，应以项目依赖脚本和团队统一方案为准。

### 2.7.3 配置 Git

把下面的姓名和邮箱替换成自己的信息：

```bash
git config --global user.name "Your Name"
git config --global user.email "you@example.com"
git config --global init.defaultBranch main
git config --global core.autocrlf input
git config --global --list
git lfs install
```

不要把 GitHub 密码、访问令牌、相机密钥或比赛内部参数写入代码仓库。

### 2.7.4 安装代码编辑器

本教材建议使用 VS Code，也可以根据团队统一要求使用 CLion。VS Code 可以从官方网站下载 Ubuntu `.deb` 包，或者通过 Snap 安装：

```bash
sudo snap install code --classic
```

建议安装以下类型的扩展：

- C/C++ 语言支持；
- Python 语言支持；
- CMake 支持；
- YAML 与 Markdown 支持；
- Git 辅助工具。

从已经加载 ROS2 环境的终端打开工程：

```bash
cd ~/Projects
code .
```

这比直接点击桌面图标更容易保证编辑器和集成终端继承正确的环境变量。

## 2.8 安装 ROS2 Humble

### 2.8.1 ROS2 安装方式

对于 Ubuntu 22.04，新生应使用 ROS 官方 apt 软件源安装二进制包。不要第一次接触 ROS2 就从源码编译整个 ROS2，也不要使用来源不明的一键安装脚本。

以下步骤依据 ROS2 官方安装方式整理，最后核对日期为 2026 年 8 月。软件源配置方式可能在未来调整，若命令失效，应以 [ROS2 Humble 官方安装文档](https://docs.ros.org/en/humble/Installation/Ubuntu-Install-Debs.html) 为准。

### 2.8.2 检查 UTF-8 区域设置

```bash
locale
```

如果输出中的 `LANG` 已经包含 `UTF-8`，通常可以继续。否则执行：

```bash
sudo apt update
sudo apt install -y locales
sudo locale-gen en_US en_US.UTF-8
sudo update-locale LC_ALL=en_US.UTF-8 LANG=en_US.UTF-8
export LANG=en_US.UTF-8
locale
```

### 2.8.3 启用 Ubuntu Universe 仓库

```bash
sudo apt install -y software-properties-common curl
sudo add-apt-repository universe
sudo apt update
```

### 2.8.4 安装 ROS2 官方软件源配置

ROS 官方目前使用 `ros2-apt-source` 包配置密钥和软件源。逐行执行：

```bash
export ROS_APT_SOURCE_VERSION=$(curl -s https://api.github.com/repos/ros-infrastructure/ros-apt-source/releases/latest | grep -F "tag_name" | awk -F'"' '{print $4}')
echo "$ROS_APT_SOURCE_VERSION"
curl -L -o /tmp/ros2-apt-source.deb "https://github.com/ros-infrastructure/ros-apt-source/releases/download/${ROS_APT_SOURCE_VERSION}/ros2-apt-source_${ROS_APT_SOURCE_VERSION}.$(. /etc/os-release && echo ${UBUNTU_CODENAME:-${VERSION_CODENAME}})_all.deb"
file /tmp/ros2-apt-source.deb
sudo dpkg -i /tmp/ros2-apt-source.deb
sudo apt update
```

正常情况下：

- `echo` 会输出一个版本号；
- `file` 应说明这是 Debian binary package，而不是 HTML 文本；
- `apt update` 的输出中会出现 ROS2 软件源。

如果版本号为空或下载到 HTML 错误页面，不要继续执行 `dpkg`。先检查 GitHub 网络访问，再重新下载。

### 2.8.5 安装 ROS2 与开发工具

```bash
sudo apt update
sudo apt install -y ros-humble-desktop ros-dev-tools
```

`ros-humble-desktop` 包含 ROS2 核心、常用工具、RViz 和演示节点；`ros-dev-tools` 包含 `colcon`、`rosdep` 等开发工具。

初始化依赖管理工具：

```bash
sudo rosdep init
rosdep update
```

如果 `rosdep init` 提示配置文件已经存在，说明它曾经初始化过。确认不是其他错误后，直接执行 `rosdep update` 即可。

### 2.8.6 加载 ROS2 环境

当前终端临时加载：

```bash
source /opt/ros/humble/setup.bash
```

验证：

```bash
echo "$ROS_DISTRO"
which ros2
ros2 --help
```

应当看到发行版为 `humble`，并且能够找到 `ros2` 命令。

为了让新终端自动加载基础 ROS2 环境，可以只追加一次：

```bash
grep -qxF 'source /opt/ros/humble/setup.bash' ~/.bashrc || echo 'source /opt/ros/humble/setup.bash' >> ~/.bashrc
source ~/.bashrc
```

不要把同一行重复追加很多次，也不要急着把每个项目工作空间都永久写入 `.bashrc`。

### 2.8.7 使用 talker 和 listener 验证

打开第一个终端：

```bash
source /opt/ros/humble/setup.bash
ros2 run demo_nodes_cpp talker
```

打开第二个终端：

```bash
source /opt/ros/humble/setup.bash
ros2 run demo_nodes_py listener
```

如果第二个终端持续收到 `Hello World` 消息，说明 ROS2 的基础安装和进程间通信正常。按 `Ctrl+C` 分别停止两个节点。

### 2.8.8 ROS_DOMAIN_ID

同一局域网中的 ROS2 计算机可能自动发现彼此。实验室里同时有多台机器人时，应为不同系统分配不同的 Domain ID：

```bash
export ROS_DOMAIN_ID=26
```

需要互相通信的上位机和节点必须使用相同的 ID；不应该互相发现的系统应使用不同 ID。具体数值由团队统一分配，不要每个人随意写进 `.bashrc`。

## 2.9 理解 ROS2 工作空间

### 2.9.1 工作空间目录

典型 ROS2 工作空间包含：

```text
ros2_ws/
├── src/       # 源代码，主要编辑这里
├── build/     # 构建过程的中间文件
├── install/   # 构建结果和环境脚本
└── log/       # 构建日志
```

第一次构建前通常只有 `src`，其他三个目录由 `colcon` 自动生成。不要手工修改 `build` 和 `install` 中的代码，因为重新构建后修改会消失。

### 2.9.2 underlay 与 overlay

ROS2 环境是逐层加载的：

```text
/opt/ros/humble                 基础 ROS2，称为 underlay
        ↓ source
项目工作空间/install/setup.bash  项目构建结果，称为 overlay
```

正确顺序是先加载基础 ROS2，再加载项目工作空间：

```bash
source /opt/ros/humble/setup.bash
source ~/Projects/example_ws/install/setup.bash
```

后加载的工作空间可以增加功能包，也可能覆盖同名功能包。很多“明明构建成功却运行旧代码”的问题，都是加载顺序或旧环境污染造成的。

### 2.9.3 创建一个最小工作空间

```bash
mkdir -p ~/ros2_practice_ws/src
cd ~/ros2_practice_ws
source /opt/ros/humble/setup.bash
colcon build
source install/setup.bash
```

空工作空间也可以完成构建。观察构建后生成的目录：

```bash
tree -L 2 ~/ros2_practice_ws
```

### 2.9.4 常用 colcon 命令

```bash
colcon list
colcon build --symlink-install
colcon build --symlink-install --packages-select package_name
colcon test --event-handlers console_direct+
colcon test-result --verbose
```

`--symlink-install` 对 Python 节点开发很方便，修改 Python 源文件后通常不必完整复制安装；但新增入口、消息或依赖后仍然需要重新构建。

## 2.10 获取并构建 RC2026 工程

### 2.10.1 规划开发目录

建议统一使用以下目录，而不是把项目长期放在桌面或下载目录：

```text
~/Projects/     代码仓库
~/Datasets/     训练与测试数据集
~/Models/       本地模型文件
~/rosbags/      ROS2 录制数据
~/Documents/    设计文档与记录
```

创建代码目录：

```bash
mkdir -p ~/Projects
cd ~/Projects
```

### 2.10.2 克隆完整仓库

RC2026 仓库包含子模块，并可能包含由 Git LFS 管理的大文件。使用：

```bash
git lfs install
git clone --recurse-submodules https://github.com/HGZQ1/GAFA-Artlnnov.RC2026.git
cd ~/Projects/GAFA-Artlnnov.RC2026
git submodule update --init --recursive
git lfs pull
git status
```

通过 `git clone` 得到的默认目录名是 `GAFA-Artlnnov.RC2026`。如果你下载的是 ZIP，目录可能带有 `-main` 后缀，不要混用两套路径。

先阅读项目说明：

```bash
less README.md
```

### 2.10.3 安装项目依赖

项目提供了主机依赖脚本。运行任何仓库脚本前，先确认自己位于正确仓库，并大致查看脚本内容：

```bash
cd ~/Projects/GAFA-Artlnnov.RC2026/ros2_vision_project
less docker/install_host_deps.sh
bash docker/install_host_deps.sh
```

然后让 `rosdep` 根据各功能包的 `package.xml` 补充依赖：

```bash
cd ~/Projects/GAFA-Artlnnov.RC2026/ros2_vision_project/ros2_ws
source /opt/ros/humble/setup.bash
rosdep install --from-paths src --ignore-src --rosdistro humble -r -y
```

`rosdep` 报错时应记录缺失依赖的准确名称。不要看到红色输出就直接用 `sudo pip` 猜测安装。

### 2.10.4 构建真机工作空间

```bash
cd ~/Projects/GAFA-Artlnnov.RC2026/ros2_vision_project/ros2_ws
source /opt/ros/humble/setup.bash
colcon build --symlink-install
source install/setup.bash
```

构建完成后检查：

```bash
colcon list
ros2 pkg list | grep -E 'vision_detector|decision_processor|rc2026'
```

如果构建失败，首先处理终端中出现的**第一处真实错误**。后续大量错误往往只是第一个功能包失败产生的连锁结果。

### 2.10.5 构建仿真工作空间

这里的“仿真”是 Gazebo 中的机器人和比赛场地，不是操作系统虚拟机。仿真工作空间与真机工作空间相互独立：

```bash
cd ~/Projects/GAFA-Artlnnov.RC2026/ros2_vision_project/sim_ws
source /opt/ros/humble/setup.bash
rosdep install --from-paths src --ignore-src --rosdistro humble -r -y
colcon build --symlink-install
source install/setup.bash
```

启动基础仿真：

```bash
ros2 launch rc2026_sim simulation.launch.py use_game_controller:=true
```

仿真可以验证节点、状态机和消息链路，但不能代替传感器、通信和机构的真机测试。

### 2.10.6 启动真机系统

只有在传感器驱动、下位机通信和设备权限都已经配置完成后，才启动完整真机系统：

```bash
cd ~/Projects/GAFA-Artlnnov.RC2026/ros2_vision_project
source /opt/ros/humble/setup.bash
source ros2_ws/install/setup.bash
python3 scripts/launch_rc2026.py
```

普通开发电脑没有连接全部设备时，完整启动出现设备错误是预期现象。此时应运行仿真或单独启动正在开发的模块，而不是伪造设备文件让错误消失。

### 2.10.7 真机专用驱动

RC2026 还需要 RealSense、Livox Mid-360S、`livox_ros_driver2` 和 FAST-LIO2 等驱动或功能包。这些组件对内核、ROS2 分支和设备固件有明确兼容要求，因此不在本章复制一组很快可能过期的安装命令。

安装时遵守以下规则：

1. 使用项目 README 指定的版本或团队保存的已验证版本；
2. 对照设备厂商和项目官方文档；
3. 安装前记录 `uname -r`、Ubuntu 版本和设备固件版本；
4. 一次只安装并验证一个设备；
5. 不要同时用 apt 和源码安装两份相同驱动；
6. 驱动验证成功后记录提交号和安装步骤。

## 2.11 连接和检查机器人设备

### 2.11.1 查看 USB 设备

插入设备前后分别执行：

```bash
lsusb
```

实时观察内核设备日志：

```bash
sudo dmesg --follow
```

插入设备后应看到厂商、USB 端口和驱动绑定信息。使用 `Ctrl+C` 停止观察。

### 2.11.2 串口权限

USB 转串口通常显示为 `/dev/ttyUSB0` 或 `/dev/ttyACM0`：

```bash
ls -l /dev/ttyUSB* /dev/ttyACM*
```

如果文件属于 `dialout` 组，把当前用户加入该组：

```bash
sudo usermod -aG dialout "$USER"
```

之后必须注销并重新登录，再检查：

```bash
groups
```

不要使用 `sudo chmod 777 /dev/ttyUSB0` 作为长期方案。设备重新插拔后权限会恢复，而且所有用户可读写串口并不安全。

### 2.11.3 稳定的设备名称与 udev

`ttyUSB0` 只代表当前枚举顺序。改变插入顺序后，STM32 和红外模块可能交换编号。正式系统应使用 udev 规则创建稳定名称，例如：

```text
/dev/r2_stm32
/dev/r1_ir_receiver
/dev/r2_ir_receiver
```

先查看设备属性：

```bash
udevadm info -a -n /dev/ttyUSB0 | less
```

下面只是规则结构示例，`idVendor`、`idProduct` 和 `serial` 必须替换为实际查询结果：

```udev
SUBSYSTEM=="tty", ATTRS{idVendor}=="xxxx", ATTRS{idProduct}=="yyyy", ATTRS{serial}=="unique-serial", GROUP="dialout", MODE="0660", SYMLINK+="r2_stm32"
```

规则通常放在 `/etc/udev/rules.d/99-rc2026.rules`，修改后重新加载：

```bash
sudo udevadm control --reload-rules
sudo udevadm trigger
```

多个 CH340 转串口模块可能拥有相同的厂商和产品 ID，甚至没有唯一序列号。此时仅按 `idVendor` 和 `idProduct` 无法区分设备，应改用带唯一序列号的转接器，或者根据固定物理 USB 端口设计规则。

### 2.11.4 检查普通相机

```bash
v4l2-ctl --list-devices
ls -l /dev/video*
```

一个物理相机可能产生多个 `/dev/video*` 节点，需要根据格式和分辨率确认实际使用哪一个，而不能只认 `/dev/video0`。

### 2.11.5 检查网络接口

```bash
ip -br address
ip route
ping -c 4 127.0.0.1
```

连接 Livox 雷达时，上位机有线网口必须与雷达位于兼容的网段。静态 IP 应以雷达说明书和团队配置为准，不要照抄其他人的地址。检查时至少回答：

- 哪个接口连接雷达；
- 上位机接口的 IPv4 地址是什么；
- 雷达地址是什么；
- 两者的子网掩码是否匹配；
- 是否出现重复 IP；
- 防火墙是否阻止所需数据。

### 2.11.6 配置 SSH 远程登录

真机调试通常通过局域网 SSH 完成：

```bash
sudo apt install -y openssh-server
sudo systemctl enable --now ssh
systemctl status ssh
hostname -I
```

另一台计算机可以连接：

```bash
ssh 用户名@上位机IP
```

第一次连接会要求确认主机指纹。确认 IP 和设备无误后再接受。真机必须使用强密码，不要把密码写入脚本或仓库。

## 2.12 管理终端环境

### 2.12.1 推荐的加载顺序

开发 RC2026 真机工作空间时，每个新终端执行：

```bash
source /opt/ros/humble/setup.bash
source ~/Projects/GAFA-Artlnnov.RC2026/ros2_vision_project/ros2_ws/install/setup.bash
```

开发仿真工作空间时改为：

```bash
source /opt/ros/humble/setup.bash
source ~/Projects/GAFA-Artlnnov.RC2026/ros2_vision_project/sim_ws/install/setup.bash
```

初学阶段不要在同一个终端反复叠加多个不相关工作空间。遇到环境混乱，最简单的恢复方法通常是关闭终端，重新打开一个干净终端，再按正确顺序加载。

### 2.12.2 不建议永久加载项目工作空间

把所有项目的 `install/setup.bash` 都写入 `.bashrc` 会造成：

- 忘记当前运行的是哪个工作空间；
- 同名包被意外覆盖；
- 工作空间被移动或尚未构建时，每次开终端都报错；
- 排查依赖问题时难以获得干净环境。

`.bashrc` 中自动加载基础 `/opt/ros/humble/setup.bash` 即可。项目工作空间应当在需要时显式加载，或者为不同项目编写单独的环境脚本。

### 2.12.3 检查当前环境

```bash
echo "$ROS_DISTRO"
echo "$ROS_DOMAIN_ID"
echo "$AMENT_PREFIX_PATH" | tr ':' '\n'
which ros2
which python3
python3 -c "import sys; print(sys.executable); print(*sys.path, sep='\n')"
```

如果路径中出现不认识的 Conda 环境、旧工作空间或不存在的目录，应先清理环境，而不是继续添加更多 `PYTHONPATH` 和 `AMENT_PREFIX_PATH`。

## 2.13 常见问题与排查

| 现象 | 优先检查 | 常见原因 |
| --- | --- | --- |
| `ros2: command not found` | `echo $ROS_DISTRO`、`which ros2` | 没有加载 `/opt/ros/humble/setup.bash` |
| 找不到自己的 ROS2 包 | 构建结果、加载顺序 | 包未构建或没有加载工作空间 |
| `colcon build` 没有发现包 | `pwd`、`find src -name package.xml` | 当前目录错误或源码不完整 |
| `Unable to locate package ros-humble-*` | Ubuntu 版本、ROS 软件源 | 不是 Jammy、软件源未配置或未 `apt update` |
| Python 模块找不到 | `which python3`、`sys.path` | 使用了错误解释器或依赖未安装 |
| 串口 `Permission denied` | `ls -l /dev/ttyUSB*`、`groups` | 用户不在 `dialout` 组或未重新登录 |
| 串口编号变化 | `udevadm info` | 依赖了不稳定的 `ttyUSB0` 编号 |
| 相机没有图像 | `lsusb`、`dmesg`、`v4l2-ctl` | USB 带宽、供电、权限或驱动问题 |
| 雷达无法连接 | `ip -br address`、`ip route` | 网段、静态 IP、网线或防火墙问题 |
| 两台 ROS2 设备互相看不到 | IP、Domain ID、网络、防火墙 | 不在同一网络或 Domain ID 不一致 |
| 构建过程突然退出 | `free -h`、`df -h /` | 内存或磁盘空间不足 |
| 修改 Python 后仍运行旧代码 | `which`、工作空间路径 | 加载了旧 overlay 或未使用 symlink install |

### 2.13.1 收集环境诊断信息

求助前执行并保存以下信息：

```bash
date
lsb_release -ds
uname -m
uname -r
python3 --version
echo "ROS_DISTRO=$ROS_DISTRO"
echo "ROS_DOMAIN_ID=$ROS_DOMAIN_ID"
which ros2
which python3
df -h /
free -h
ip -br address
lsusb
git -C ~/Projects/GAFA-Artlnnov.RC2026 rev-parse HEAD
git -C ~/Projects/GAFA-Artlnnov.RC2026 status --short
```

同时提供：

- 你执行的完整命令；
- 从第一行开始的完整错误输出；
- 问题能否稳定复现；
- 当前是否连接真机设备；
- 其他成员在相同提交上是否正常。

### 2.13.2 不要用“清空重装”代替定位

重新安装系统有时能让问题暂时消失，但也会销毁现场，导致团队永远不知道问题为什么发生。除非系统已经无法可信地修复，否则应先定位是：

```text
操作系统 -> 软件源 -> ROS2 基础环境 -> 项目依赖 -> 工作空间构建
        -> 环境加载 -> 设备权限 -> 网络 -> 程序逻辑
```

每次只验证一层。这样得到的解决方法才能写进团队文档，并在下一台电脑上复用。

## 2.14 本章小结

本章建立了 RC2026 上位机的统一开发基线：原生 Ubuntu 22.04、ROS2 Humble、系统 Python 3.10，以及通过 `colcon` 构建的真机和仿真工作空间。

环境搭建中最重要的并不是记住所有安装命令，而是理解以下关系：

```text
Ubuntu 版本
    ↓ 决定
ROS2 发行版与系统依赖
    ↓ 提供
工作空间构建环境
    ↓ 生成
项目功能包与运行环境
    ↓ 连接
相机、雷达、串口和下位机
```

版本、路径、权限、网络和环境加载顺序都必须明确。发现问题时，从底层环境向上逐层验证，比反复重装和随意修改配置更可靠。

## 2.15 思考与练习

1. 为什么 RC2026 项目不应直接改用 Ubuntu 24.04 或 26.04？
2. 分别解释 `/opt/ros/humble/setup.bash` 和工作空间 `install/setup.bash` 的作用。
3. 创建 `~/ros2_practice_ws`，完成空工作空间构建，并说明四个目录的用途。
4. 打开两个终端，运行 ROS2 talker 和 listener，再使用 `ros2 node list` 与 `ros2 topic list` 观察它们。
5. 插入一个 USB 设备，用 `lsusb` 和 `dmesg --follow` 找出它的变化。
6. 解释为什么 `/dev/ttyUSB0` 不适合作为比赛系统中的永久设备名称。
7. 克隆 RC2026 仓库，找出真机工作空间、仿真工作空间和系统启动入口。
8. 模拟一次环境问题，整理一份包含系统版本、ROS2 环境、错误命令和完整输出的问题报告。

### 本章验收标准

只有满足以下条件，才算完成开发环境搭建：

- [ ] 原生系统为 Ubuntu 22.04 LTS 64 位；
- [ ] 系统更新完成，时区和时间同步正常；
- [ ] 能熟练使用 `pwd`、`ls`、`cd`、`mkdir`、`cp`、`mv` 和 `less`；
- [ ] Git、CMake、GCC、Python 3.10 可以正常运行；
- [ ] ROS2 talker 与 listener 可以正常通信；
- [ ] 能解释基础 ROS2 与项目工作空间的加载顺序；
- [ ] RC2026 仓库及其子模块、LFS 文件获取完整；
- [ ] 至少一个 RC2026 工作空间构建成功；
- [ ] 知道如何检查 USB、串口、相机和网络接口；
- [ ] 能提交一份包含完整环境信息的故障报告。

## 2.16 参考资料

- [Ubuntu Desktop 官方安装教程](https://ubuntu.com/tutorials/install-ubuntu-desktop)
- [Ubuntu 22.04 LTS 官方镜像](https://releases.ubuntu.com/jammy/)
- [ROS2 Humble Ubuntu 安装文档](https://docs.ros.org/en/humble/Installation/Ubuntu-Install-Debs.html)
- [ROS2 发行版与支持周期](https://docs.ros.org/en/humble/Releases.html)
- [ROS 官方 apt 软件源配置项目](https://github.com/ros-infrastructure/ros-apt-source)
- [Intel RealSense Linux 安装说明](https://docs.ros.org/en/ros2_packages/humble/api/librealsense2/doc/installation.html)
- [GAFA-Artlnnov.RC2026 上位机项目](https://github.com/HGZQ1/GAFA-Artlnnov.RC2026)
