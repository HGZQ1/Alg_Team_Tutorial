# 第四章 数据结构基础和算法基础

算法组经常会听到“算法”这个词。算法不是某一段神秘的代码，而是解决问题的一组明确步骤；数据结构则决定这些步骤如何组织和访问数据。

## 本章学习目标

- 理解数据、数据元素、数据项、数据对象和数据结构等基本术语；
- 能够区分逻辑结构和存储结构；
- 看懂数组、链表、栈、队列、树和图的基本示意图；
- 理解抽象数据类型（ADT）的含义，并能写出简单的 ADT 说明；
- 使用 C++ 将一个线性表 ADT 实现为可运行的类。

## 4.1 数据结构绪论

计算机解决问题通常要经过以下几个步骤：**分析问题，抽象出数据模型；选择合适的数据结构；设计解决问题的算法；编写、测试和调试程序**。数据结构并不只用于非数值问题，坐标、速度、点云等数值数据同样需要用数组、队列、树或图来组织。

在机器人开发中，传感器消息可以放进队列，地图可以表示为图，比赛流程可以表示为状态机，点云和图像则通常以数组或矩阵保存。数据结构选得合适，程序会更容易编写、调试和扩展；选得不合适，即使算法思想正确，程序也可能运行缓慢或难以维护。

下面用一个数据管理例子说明数据结构的作用。

### 学生学籍管理系统
|学号|姓名|性别|专业|
|---|---|---|---|
|2425122|张三|男|智能交互|
|2425123|李四|男|智能交互|
|2425137|王五|女|智能交互|
|2425138|琴羽|女|计算机科学与技术|

这个例子中，每一行学生记录可以看作一个**数据元素**，学号、姓名、性别和专业是这个元素的不同**数据项**。系统需要支持添加学生、删除学生、按学号查找、修改专业和遍历所有记录等操作。数据结构要解决的就是：数据如何组织，以及这些操作如何高效完成。

## 4.2 数据结构的基本概念和术语

### 4.2.1 数据、数据元素和数据项

| 术语 | 含义 | 学籍管理中的例子 |
| --- | --- | --- |
| 数据（Data） | 能被计算机识别和处理的信息 | 全体学生信息 |
| 数据元素（Data Element） | 组成数据的基本单位，通常作为一个整体处理 | 张三这一条学生记录 |
| 数据项（Data Item） | 相对于当前问题可视为不可再分或具有独立含义的最小单位 | 学号、姓名、专业 |
| 数据对象（Data Object） | 性质相同的数据元素的集合 | 全体学生记录的集合 |
| 数据结构（Data Structure） | 数据元素之间的逻辑关系、具体的存储表示，以及允许进行的操作 | 按学号顺序保存学生记录的线性表 |
| 数据类型（Data Type） | 一组值和允许在这些值上进行的操作 | `int`、`float`、字符串、列表 |
| 操作（Operation） | 对数据进行的处理动作 | 查找、插入、删除、修改和遍历 |

不要把“数据结构”和“数据类型”混为一谈。`int` 规定一个整数能表示什么以及能进行什么运算；列表则规定多个元素怎样排列，以及怎样访问和修改这些元素。

### 4.2.2 逻辑结构和存储结构

同一组数据有两个层面的描述：

- **逻辑结构**：只描述元素之间的关系，不关心它们在内存中的具体位置；
- **存储结构（物理结构）**：描述数据实际如何放在内存中，以及如何通过指针或索引找到下一个元素。

例如，`A、B、C、D` 组成一个线性表，这是逻辑结构；把它们放在连续的内存单元中是顺序存储，把它们放在不同位置并用指针连接是链式存储。

| 对比项 | 逻辑结构 | 存储结构 |
| --- | --- | --- |
| 关注内容 | 元素之间是什么关系 | 元素在内存中如何保存 |
| 是否依赖地址 | 不依赖 | 依赖内存地址、指针或索引 |
| 常见形式 | 集合、线性结构、树、图 | 顺序存储、链式存储、索引存储、散列存储 |
| 例子 | “每个节点最多有两个子节点” | 用数组或指针保存二叉树节点 |

逻辑结构和存储结构不是一一对应的。同一种逻辑结构可以有多种实现，例如线性表既可以用数组实现，也可以用链表实现；同一种存储方式也可以服务于不同的逻辑结构。

### 4.2.3 常见数据操作

学习一个数据结构时，不能只问“它长什么样”，还要问“它支持哪些操作”：

| 操作 | 含义 |
| --- | --- |
| 访问（Access） | 根据位置或键取得元素 |
| 插入（Insert） | 在指定位置加入新元素 |
| 删除（Delete） | 移除某个元素 |
| 更新（Update） | 修改已有元素的值 |
| 查找（Search） | 根据位置、键或条件寻找元素 |
| 遍历（Traverse） | 按一定顺序访问全部元素 |
| 排序（Sort） | 按关键字重新排列元素 |
| 合并（Merge） | 将多个同类结构组合起来 |

设数据元素个数为 `n`，我们常用大 O 记号描述操作随 `n` 增长的速度：

- `O(1)`：操作次数基本不随 `n` 变化，例如数组按下标访问；
- `O(n)`：最多需要检查所有元素，例如无序列表查找；
- `O(log n)`：每次把问题规模缩小，例如在有序且支持随机访问的数组中二分查找；
- `O(n²)`：某些嵌套遍历的常见最坏情况上界，数据量大时通常较慢；嵌套循环并不必然是 `O(n²)`。

复杂度是比较算法和数据结构的重要工具，但实际运行还会受到缓存、内存带宽、编程语言和硬件等因素影响。

### 4.2.4 数据结构的分类图

下面的分类图用于建立整体印象。实际工程中，一个结构可能同时具有多种特征，例如“用链表实现的树”。

```text
数据结构
|
+-- 按逻辑关系
|   |
|   +-- 集合：元素之间没有特定顺序关系
|   +-- 线性结构：元素排成一条序列
|   |   +-- 线性表（抽象数据类型）
|   |   |   +-- 顺序表：用连续存储实现
|   |   |   +-- 链表：用节点和引用实现
|   |   |   +-- 栈、队列：受限的线性表
|   |
|   +-- 非线性结构
|       +-- 树：一对多的层次关系
|       +-- 图：多对多的连接关系
|
+-- 按存储方式
    |
    +-- 顺序存储：元素放在连续位置
    +-- 链式存储：节点通过指针或引用连接
    +-- 索引存储：额外保存查找索引
    +-- 散列存储：通过哈希函数定位元素
```

### 4.2.5 常见数据结构示意图

#### 线性表与顺序表（数组）

线性表的元素具有唯一的先后顺序。顺序表或数组通常把元素放在连续内存中，因此可以通过下标快速访问。在 C++ 中，`std::vector` 的元素存储也是连续的；如果容器保存的是指针，连续的只是指针槽位，不一定是对象本身：

```text
下标       0       1       2       3
       +-------+-------+-------+-------+
数组   |   A   |   B   |   C   |   D   |
       +-------+-------+-------+-------+
内存   |连续地址|连续地址|连续地址|连续地址|
```

数组的优点是按下标访问快，缺点是在中间插入或删除时，后面的元素通常需要整体移动。

#### 单链表

链表由多个节点组成。下面画的是单链表；双向链表还会在节点中保存指向前一个节点的 `prev` 引用。每个节点保存数据和下一个节点的引用，节点不要求放在连续内存中：

```text
+-------+-------+     +-------+-------+     +-------+-------+
| data A| next  | --> | data B| next  | --> | data C| None  |
+-------+-------+     +-------+-------+     +-------+-------+
```

链表插入和删除节点时不必整体移动数据，但要从头节点沿着引用逐个查找，所以按位置访问通常较慢。

#### 栈（Stack）

栈遵循 **后进先出（LIFO）**：

```text
        +-------+
栈顶 -> |   C   |  <- 最后压入，最先弹出
        +-------+
        |   B   |
        +-------+
        |   A   |
        +-------+
```

常见操作是 `push`（压栈）和 `pop`（出栈）。函数调用、撤销操作和深度优先搜索都可以使用栈。

#### 队列（Queue）

队列遵循 **先进先出（FIFO）**：

```text
出队方向 <---  +-------+-------+-------+  <--- 入队方向
              |   A   |   B   |   C   |
              +-------+-------+-------+
              A 从左侧离开，新元素从右侧进入
```

机器人中的传感器消息缓存、任务等待队列和生产者/消费者模型都经常使用队列。

#### 树（Tree）

树表示层次关系。没有父节点的节点叫根节点，没有子节点的节点叫叶节点：

```text
             A                 <- 根
           /   \
          B     C              <- 子节点
         / \     \
        D   E     F            <- 叶节点：D、E、F
```

树的常见术语包括：根、父节点、子节点、兄弟节点、叶节点、层、深度、高度和子树。根节点通常位于第 0 层；节点所在层数可以理解为它到根的边数，树的高度是从根到叶节点的最长路径长度。文件系统、行为树、二叉搜索树和 ROS2 的坐标变换树都可以用树来理解；TF 是一棵带根的坐标变换树，不能形成环。

#### 图（Graph）

图由顶点和边组成，可以表示任意连接关系：

```text
      A -------- B
      |          |
      |          |
      C -------- D
```

图中的基本术语包括：顶点（vertex）、边（edge）、邻接和路径。边有方向时是有向图，没有方向时是无向图；边带有距离或代价时是带权图，否则是无权图。图中从一个顶点沿边回到自身形成环；在无向图中，任意两点之间存在路径的最大部分称为连通分量，有向图还可以进一步讨论强连通分量。地图导航、道路网络、机器人通信拓扑和任务依赖关系都可以建模为图。

### 4.2.6 机器人中的数据结构例子

| 机器人问题 | 适合的数据结构 | 原因 |
| --- | --- | --- |
| 连续保存最近 10 帧图像 | 队列或环形缓冲区 | 新数据进入，旧数据自动移出 |
| 比赛流程和模式切换 | 枚举值 + 状态机 | 当前状态和允许的转换关系清晰 |
| 地图中的路口和道路 | 图 | 顶点表示位置，边表示可通行路线 |
| 机器人坐标变换 | 树 | 一个坐标系通常挂在另一个坐标系下面 |
| 像素、深度和点云 | 数组或矩阵 | 元素位置与下标有直接对应关系 |
| 根据设备名称查找串口 | 哈希表/字典 | 可以通过键快速定位值 |

## 4.3 抽象数据类型（ADT）

### 4.3.1 为什么需要抽象

如果一个程序直接依赖“数据放在哪个数组下标”或“节点用了哪个指针”，更换实现时就要修改大量业务代码。抽象数据类型把“使用规则”和“内部实现”分开：

- 使用者只需要知道**能做什么**以及**如何调用**；
- 实现者负责决定**内部如何保存**以及**如何完成操作**。

例如，排队系统只需要知道“加入队尾”和“从队首取出”，不应该要求上层程序了解队列是用数组、链表还是环形缓冲区实现的。

### 4.3.2 ADT 的定义

抽象数据类型（Abstract Data Type，ADT）是：

> 一个数学模型，以及定义在这个模型上的一组操作；它描述数据的逻辑特征和操作规则，但不规定具体的存储方式和编程语言。

ADT 可以分成三部分：

1. **数据对象**：有哪些数据；
2. **数据关系**：数据元素之间如何组织；
3. **操作集合**：允许对数据做什么，以及输入条件和结果是什么。

可以把程序分成四层：

```text
问题需求
   |
   v
ADT 说明：定义“数据是什么、能做什么”
   |
   v
具体实现：数组、链表、哈希表或其他存储方式
   |
   v
应用代码：ROS2 节点、视觉模块、决策模块
```

ADT 不是某个特定的类。在 C++ 中，类、结构体和模板都可以用来实现 ADT；同一个 ADT 也可以由多个不同的类实现。

### 4.3.3 用形式化方式表达线性表 ADT

下面用 List<T> 表示元素类型为 T 的线性表。尖括号中的 T 表示任意元素类型，可以是整数、字符串，也可以是机器人消息对象。

```text
ADT List<T>

数据对象：
    由 n 个同类型元素组成的有限序列
    L = <a0, a1, ..., a(n-1)>
    元素有先后次序，允许重复

基本操作：
    create() -> List
        创建并返回一个空表

    length(L) -> int
        返回表中元素个数

    is_empty(L) -> bool
        判断表是否为空

    get(L, i) -> T
        返回下标 i 处的元素
        前置条件：0 <= i < length(L)

    set(L, i, x)
        将下标 i 处的元素修改为 x
        前置条件：0 <= i < length(L)

    insert(L, i, x)
        在下标 i 处插入 x
        前置条件：0 <= i <= length(L)

    remove(L, i) -> T
        删除并返回下标 i 处的元素
        前置条件：0 <= i < length(L)

    find(L, x) -> int
        返回 x 第一次出现的下标；不存在时返回 -1

    traverse(L)
        按从前到后的顺序访问每个元素
```

操作说明除了前置条件，还应写清楚操作完成后的结果：

| 操作 | 典型后置条件 |
| --- | --- |
| insert(L, i, x) | 表长度增加 1，原来位置 i 及之后的元素向后移动，位置 i 存放 x |
| remove(L, i) | 表长度减少 1，返回原位置 i 的元素，其后元素向前移动 |
| set(L, i, x) | 表长度不变，位置 i 的值变为 x |
| get(L, i) | 表本身不改变，返回位置 i 的元素 |

前置条件必须在调用操作前满足。如果调用者传入了非法下标，实现可以抛出异常或返回错误；本节的 C++ 示例采用抛出 std::out_of_range 的方式，让错误尽早暴露。

### 4.3.4 在 C++ 中表达 ADT 接口

在 C++ 中，可以先写出只描述操作的抽象接口。接口不说明数据到底用数组还是链表保存：

```cpp
class IntListInterface {
public:
    virtual ~IntListInterface() = default;

    virtual int size() const = 0;
    virtual bool empty() const = 0;
    virtual int get(int index) const = 0;
    virtual void set(int index, int value) = 0;
    virtual void insert(int index, int value) = 0;
    virtual void push_back(int value) = 0;
    virtual int remove_at(int index) = 0;
    virtual int find(int value) const = 0;
};
```

代码中的 virtual ... = 0 表示纯虚函数：

- 只规定函数名称、参数和返回值；
- 不提供具体实现；
- 具体的数据结构类必须实现这些函数；
- 上层代码可以依赖这个接口，而不依赖某一种存储方式。

这里先使用 int 是为了让接口容易理解。实际项目中可以使用 C++ 模板，把元素类型写成 T。

### 4.3.5 用顺序存储实现线性表

下面使用 C++ 标准库的 std::vector<int> 作为底层连续存储，实现上面的 ADT。std::vector 会自动管理动态数组的内存，适合先学习接口和操作。

```cpp
#include <iostream>
#include <stdexcept>
#include <vector>

class IntListInterface {
public:
    virtual ~IntListInterface() = default;
    virtual int size() const = 0;
    virtual bool empty() const = 0;
    virtual int get(int index) const = 0;
    virtual void set(int index, int value) = 0;
    virtual void insert(int index, int value) = 0;
    virtual void push_back(int value) = 0;
    virtual int remove_at(int index) = 0;
    virtual int find(int value) const = 0;
};

class ArrayList : public IntListInterface {
private:
    std::vector<int> data_;

    void check_index(int index) const {
        if (index < 0 || index >= size()) {
            throw std::out_of_range("元素下标超出范围");
        }
    }

    void check_insert_index(int index) const {
        if (index < 0 || index > size()) {
            throw std::out_of_range("插入下标超出范围");
        }
    }

public:
    int size() const override {
        return static_cast<int>(data_.size());
    }

    bool empty() const override {
        return data_.empty();
    }

    int get(int index) const override {
        check_index(index);
        return data_[index];
    }

    void set(int index, int value) override {
        check_index(index);
        data_[index] = value;
    }

    void insert(int index, int value) override {
        check_insert_index(index);
        data_.insert(data_.begin() + index, value);
    }

    void push_back(int value) override {
        data_.push_back(value);
    }

    int remove_at(int index) override {
        check_index(index);
        int removed = data_[index];
        data_.erase(data_.begin() + index);
        return removed;
    }

    int find(int value) const override {
        for (int i = 0; i < size(); ++i) {
            if (data_[i] == value) {
                return i;
            }
        }
        return -1;
    }

    void print() const {
        for (int value : data_) {
            std::cout << value << ' ';
        }
        std::cout << '\n';
    }
};

int main() {
    ArrayList students;

    students.push_back(2425122);
    students.push_back(2425123);
    students.insert(1, 2425137);

    std::cout << students.get(0) << '\n';
    std::cout << students.find(2425137) << '\n';

    students.set(0, 2425000);
    students.remove_at(1);
    students.print();

    return 0;
}
```

将代码保存为 list_demo.cpp 后编译：

```bash
g++ -std=c++17 -Wall -Wextra list_demo.cpp -o list_demo
./list_demo
```

代码中的关键点：

- data_ 是类的私有成员，外部代码不能直接修改它；
- public 部分提供 ADT 的操作，形成稳定的使用接口；
- check_index 和 check_insert_index 检查操作的前置条件；
- override 表示子类正在实现父类的纯虚函数；
- std::vector 是当前实现方式，换成链表后，外部调用方式仍可以保持不变。

### 4.3.6 同一个 ADT 可以有不同实现

线性表的逻辑关系始终是：

```text
a0 -> a1 -> a2 -> ... -> a(n-1)
```

它可以用连续数组表示：

```text
[ a0 ][ a1 ][ a2 ][ a3 ]
```

也可以用链表表示：

```text
[ a0 | next ] -> [ a1 | next ] -> [ a2 | next ] -> None
```

两种实现都满足 List<T> 的 ADT 规则，但性能不同：

| 操作 | 数组/动态数组 | 链表 |
| --- | --- | --- |
| 按下标访问 | O(1) | O(n) |
| 已知位置插入 | 通常 O(n)，需要移动元素 | 修改链接可为 O(1) |
| 查找某个值 | O(n) | O(n) |
| 末尾追加 | 动态数组通常为均摊 O(1) | 保存尾指针时可为 O(1) |
| 连续访问 | 快，缓存友好 | 节点分散，缓存不友好 |
| 额外空间 | 较少 | 每个节点需要保存引用 |

这就是“抽象”和“实现”分离的价值：业务代码依赖 List<T> 的规则，底层可以根据性能、内存和设备环境进行替换。

### 4.3.7 ADT 实现时的检查清单

实现一个新的数据结构时，可以按以下顺序检查：

1. 写出数据对象和元素之间的关系；
2. 列出必须提供的操作；
3. 写清楚每个操作的输入、输出和边界条件；
4. 选择顺序、链式、索引或散列存储方式；
5. 为每个操作分析时间复杂度和额外空间；
6. 用空数据、一个元素、最大容量和非法输入进行测试；
7. 再把它接入 ROS2 节点或其他业务模块。

不要一开始就写代码。先写 ADT 说明，能减少接口反复修改，也能让组内成员更容易协作。

## 4.4 本章小结

数据结构回答“数据怎样组织”，算法回答“问题怎样解决”。学习时建议遵循以下顺序：

```text
问题 -> 抽象数据对象和关系 -> 选择数据结构
     -> 定义 ADT 操作 -> 选择具体实现
     -> 分析复杂度 -> 编写和测试算法
```

在后续章节中，我们会在此基础上学习线性表、栈、队列、树、图，以及查找和排序算法。
