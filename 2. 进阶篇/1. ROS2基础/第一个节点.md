# ROS概论 

**ROS（Robot Operating System,机器人操作系统），ROS包含大量搭建机器人所需的软件和工具，是目前最广泛的机器人操作系统**。为了让你更好的理解ROS与机器人的关系，我们来做个比喻，一个温度传感器可以测量温度（相当于人类皮肤）,一个电机可以执行动作（相当于肌肉关节），然后上位机的决策系统就像人类的大脑，温度传感器把温度信号传给决策系统，决策系统做出判断，然后控制电机旋转，而ROS就充当着这其中信号交流的作用，把温度信号传给决策系统，把运动信号传给关节电机，所以ROS就相当于机器人的神经网络。 

<img src="../../picture1\微信图片_20260924184100_80_1.jpg" alt="D435i" style="width: 250px; height: 200px;">  

与单片机的通信不同，ROS2因为是部署于强大的算力平台，所以它不再局限于控制电机运动/接收反馈，可以完美传递图像数据，高频率/复杂的外接传感器数据（雷达），内部多模块（决策系统，感知系统，通信系统）的相互通信，且内部的工具软件支持数据可视化（可以看见深度相机的RGB-D所有图像，YOLO检测效果，雷达建图效果，雷达里程计等等），且支持动态仿真，让机器人的开发更为简单便捷。所以**ROS2本质就是用于快速搭建机器人的软件库（核心功能是通信）和工具集。**  

上面说了ROS2的核心功能是通信。
## ROS2通信机制 
ROS2的四大通信机制分别是**话题通讯（Topic）、服务通信（Service）、参数通信（Parameter）、动作通信（Action）。** 
- **话题通信**：是指发布 - 订阅（Publish-Subscribe）,即发布者将话题发布到某个话题上，订阅者订阅话题即可获取数据，属于单向传递。例如可以通过将机器人的位置信息发布到某个名称的话题，订阅了该话题的节点就可以获取机器人的位置数据了。人话：微信公众号。（也是最常用的通信）
- **服务通信**：分为客户端和服务端，客户端可以发送请求到服务端，由服务端处理并返回结果，属于双向传递。人话:网站访问 
- **动作通信**：属于双向传递，跟服务通信类似，同样有动作客户端和动作服务端，相比起服务通信，动作通信服务端可以在处理客户端请求时随时反馈处理进度和取消服务，动作通信往往用于复杂的机器人行为。
- **参数通信**：机器人参数的设置和读取 

## 常用工具/软件 
- **rqt**: 用于可视化图表、图像数据 
- **RViz2**: 用于三维可视化，可查看雷达建图点云、机器人路径、TF坐标变换、地图、机器人位姿、相机话题...
- **TF工具**：用于运动学坐标转换与管理 
- **URDF**：用于描述机器人的结构、关节、传感器等信息的文件格式 
- **Gazebo**: 仿真工具 
- **Nav2**:导航框架 
- **Moveit2**:机械臂运动规划应用框架 

相信在前面配置环境的时候已经安装好**ROS2-Humble**了。

以下是ROS2发行版本及对应的Ubuntu版本：

| 发行版本 | 对应Ubuntu版本 |
| --- | --- |
| Foxy Fitzroy（已停止支持） | Ubuntu 20.04 LTS |
| Galactic Geochelone（已停止支持） | Ubuntu 20.04 LTS |
| Humble Hawksbill（LTS，本教材使用） | Ubuntu 22.04 LTS |
| Iron Irwini（已停止支持） | Ubuntu 22.04 LTS |
| Jazzy Jalisco（LTS） | Ubuntu 24.04 LTS |
| Kilted Kaiju（非LTS） | Ubuntu 24.04 LTS |
| Lyrical Luth（LTS） | Ubuntu 26.04 LTS |
| Rolling Ridley（滚动开发版） | 不固定，随官方当前开发目标变化 | 

有必要再提醒一句：**ROS2 发行版与 Ubuntu 版本不是随意组合的。所以别想着用ubuntu22.04安装Ubuntu-Jazzy,切换ROS2发行版和升级发行版时需要全开发组统一升级和切换，在切换前必须提前调查好当前版本的资料和开源社区是否充足完善，软硬件是否支持是否兼容，别他妈换完后发现硬件不支持，社区生态未发展起来，那你们就等着自己所有东西都自己写吧**

下面还有其他版本的安装（如果你的Ubuntu不是22.04或想要升级ROS2开发版本）： 

ROS2-Jazzy:

